This is the web interface for the data.

You'll need to do this:

gem install --no-rdoc --no-ri \
        crack \
        curb \
        dm-core \
        dm-migrations \
        dm-sqlite-adapter \
        json \
        shotgun \
        sinatra \
        thin \
        transparency_data \

